# AGENTS

## 长期约定

### 每日训练总结

- 与“每日训练总结 / 每日复盘 / 当日学习记录”相关的内容，默认写入 `src/Training/日期/日期.log`
- 日期格式统一为 `YYYY.MM.DD`
- 总结优先基于“当天实际修改过的源码文件”，不要只看仓库全局
- 如果当天同时涉及题目分析、代码调试、知识点复习，尽量区分：
  - 今天写了什么代码
  - 今天练了什么知识
  - 今天暴露了什么问题
  - 明天建议做什么
- 若没有特别说明路径，默认不要写到根目录单独的 `training/`
- 处理这类任务时，先读本文件约定，再执行记录

推荐结构：
- `## 今日主题`
- `## 今天练到的知识`
- `## 今天暴露出来的问题`
- `## 今天的整体判断`
- `## 今天的收获`
- `## 需要回看的内容`
- `## 明日建议`

## 工作约定

### 交流与执行

1. 默认中文；先给结论，再给必要解释；不要堆砌内容。
2. 带步骤的问题一次只给当前一步；先确认状态，再给下一步。
3. 每一步都尽量可验证：说明命令、预期输出、成功或失败的判断方式。
4. 在终端场景下优先给 CLI 方案；需要命令时优先给可直接执行的纯命令。
5. 不确定环境时先检查，不要假设已经配置好。
6. 看到报错时先定位根因，再给修复动作；不要先抛一大串可能性。
7. 修改配置或代码前，先说明准备改什么；改完后说明改了哪些文件和原因。
8. 优先最小修复，不默认大改或顺手重构；危险操作、删除、覆盖、批量替换前先确认。

### 工程偏好

1. 常用 Java / Kotlin / C++；解释时注意工程结构、构建链、依赖关系和运行入口。
2. 优化建议必须真实改善复杂度、常数或可维护性；不要给表面优化。
3. Debug 默认排查顺序：环境 -> 路径 -> 权限 -> 网络 -> 版本 -> 配置 -> 逻辑。
4. 解释抽象概念时，优先给最小可运行例子或最小可复现例子。
5. 能先读项目约定就先读；不要一上来大范围重构。

### 题目文档

1. 与算法题、竞赛题相关的题面翻译、题意解释、题解推导、代码解析、复盘，默认同步写入源码同目录、同文件名的 `.md` 文件；例如 `kk.cpp` 对应 `kk.md`。
2. 如果同名 `.md` 已存在，默认增量更新，保持单一版本。
3. 题解默认写成短题解：保留题面翻译、核心结论、代码关键点、易错点、复杂度；除题面外尽量控制在 100 行以内，除非明确要求详细版。
4. 文档偏长或分节较多时，在标题后加入 `## 目录`；目录至少列出主要二级标题。
5. 目录跳转默认使用显式 HTML 锚点，并使用 ASCII 风格锚点名，例如 `<a id="core-conclusion"></a>`。
6. 题意理解、公式或结论有修正时，要同步回对应的同名 `.md`。
7. 如果是在终端里追问题目相关内容，仍按既有约定回答：先结论，再当前一步；需要命令时只给当前一步所需的最小命令。

### 公式与排版

1. 终端回答里禁止使用任何 LaTeX 表达式；公式、条件、转移、复杂度一律使用 Unicode 数学符号、ASCII 或更形象的自然语言表述。
2. 终端回答要优先保证可读性、颜色和高亮正常显示，不要使用会破坏终端渲染的写法。
3. 终端里如果需要展示推导式、状态转移、等价变形，保持“自然语言 + 纯文本推导”的表达风格不变；只是优先把这类内容放进带语法高亮的 fenced code block 里显示，不要退化成没有高亮的大段纯文本。
4. 终端里的求和、范围、下标表达优先写成线性形式，例如 `Σ(i, 1..n)`，不要默认写成 `∑_{i=1}^n` 这种不够终端友好的样式。
5. 终端里优先直接使用稳定的 Unicode 数学符号，例如 `⌈ ⌉`、`⌊ ⌋`、`≤`、`≥`、`≠`、`≈`、`∈`、`⊂`、`∪`、`∩`、`⇒`、`⇔`、`|`、`∤`；不要为了“更像公式”而退回 LaTeX。
6. `.md` 文档中可以使用 Markdown + LaTeX：行内公式用 `$...$`，独立公式用 `$$...$$`。
7. 不要在单行里直接使用 `$$...$$`；句内、列表项、编号项里的公式一律使用单个 `$...$`。
8. 如果使用 `$$...$$`，公式块前后都要换行，且公式块必须单独占行，不能和正文共享一行。
9. 不要把 `$$...$$` 塞进一行文字中间，也不要把独立公式挂在一句未结束的话后面。

默认终端公式模板：

A. Unicode 数学符号
f(x) = x² + 2x + 1
x ∈ S, A ⊂ B, a ≤ b
Σ(i, 1..n) = n(n+1)/2

B. 等宽竖排分式
      x + 1
f(x)= -----
      x - 1

C. 等宽分段函数
f(x) = { x²,   x ≥ 0
       { -x,   x < 0

D. Unicode 盒线矩阵
┌      ┐
│ a  b │
│ c  d │
└      ┘

E. 终端友好的 DP 写法
dp[i] = min(
    dp[i-1] + a[i],
    dp[i-2] + b[i]
)

F. 常用终端数学记号
⌈x/2⌉
⌊m / g(d)⌋ + 1
d | k²
d ∤ k
a ⇒ b
a ⇔ b
gcd(a,b), lcm(a,b)
v_p(k) ≥ ⌈e/2⌉

后续如果有公式推导、状态转移、矩阵、分式、分段函数、上下取整、整除、等价变形等内容，默认优先复用以上样式，并放在带高亮的 fenced code block 中展示。

### 强制执行短提示

按 AGENTS.md 执行，终端回答不用 LaTeX，公式按终端模板输出并保持高亮和颜色。
